<%if (format === 'text') { %>

Dear Cloudron Admin,

The Cloudron <%= fqdn %> with IP <%= ip %> is blacklisted in one
or more servers:

<%- JSON.stringify(info, null, 2);  %>

Please find out why the IP is in the blacklist. Email delivery will
be unreliable if the IP continues to be blacklisted.

Thank you,
Your Cloudron

<% } else { %>

<% } %>
